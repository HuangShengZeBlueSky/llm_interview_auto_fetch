# LLMs_interview_notes：十四、大模型（LLMs）agent 面

> 来源分组：LLMs_interview_notes
> 本页题目数：13
> 每题均包含基础知识补充、详细解答和案例模拟。

## 大模型（LLMs）agent 面

### 1. 一、什么是 大模型（LLMs）agent？

- 主标签：Agent与工具调用
- 来源条数：1
- 答案生成方式：模型自动补全
- 来源：[LLMs_interview_notes / 大模型（LLMs）agent 面 / 未知](https://articles.zsxq.com/id_le02luntesap.html)

### 基础知识补充

- Agent是具备自主感知决策和执行能力的智能体。
- 大语言模型作为Agent的大脑提供核心推理与规划能力。
- 闭环系统通过环境反馈不断调整策略以完成复杂任务。

### 详细解答

结论：LLM Agent是以大语言模型为核心大脑，能够自主感知环境、制定计划并调用工具执行任务的智能系统。原理：传统的LLM主要被动响应用户提示，而Agent赋予了模型主动性。它将LLM的自然语言理解和逻辑推理能力与外部环境连接起来。当接收到复杂目标时，Agent能够自主将其拆解为可执行的步骤，通过调用外部工具（如搜索引擎、API、代码解释器）获取实时信息或产生实际影响，并根据执行结果的反馈进行自我修正，直到完成最终目标。工程权衡：Agent极大地扩展了LLM的应用边界，使其从“文本生成器”进化为“任务解决者”。但其自主性也带来了不可控风险，如陷入死循环、工具调用失败或产生幻觉。因此，在工程实现中需要设计严格的护栏和人工介入机制。

### 案例模拟

面试官追问：LLM Agent和传统的RAG系统有什么本质区别？回答：RAG（检索增强生成）本质上是一个单向的流水线：检索信息然后生成回答，模型是被动接收上下文的。而Agent是一个动态闭环系统，它不仅能决定“是否需要检索”、“检索什么关键词”，还能根据检索结果决定“是否需要再次检索”或“调用其他工具”。Agent具备自主规划和多轮迭代的能力，而RAG只是Agent可以调用的众多工具之一。

### 2. 二、大模型（LLMs）agent 有哪些部分组成？

- 主标签：Agent与工具调用
- 来源条数：1
- 答案生成方式：模型自动补全
- 来源：[LLMs_interview_notes / 大模型（LLMs）agent 面 / 未知](https://articles.zsxq.com/id_le02luntesap.html)

### 基础知识补充

- 规划模块负责任务分解逻辑推理和自我反思修正。
- 记忆模块分为短期上下文记忆和长期向量数据库记忆。
- 工具调用模块使Agent能够与外部环境API进行交互。

### 详细解答

结论：LLM Agent通常由大脑（LLM）、规划（Planning）、记忆（Memory）和工具使用（Tool Use）四个核心部分组成。原理：1. 大脑：即底层的LLM，提供核心的语义理解和推理能力。2. 规划：Agent将复杂任务拆解为子任务，并制定执行步骤，同时在执行过程中进行自我反思和策略调整。3. 记忆：短期记忆通常指当前对话的上下文，长期记忆则通过向量数据库存储历史经验和知识，供随时检索。4. 工具使用：Agent通过调用外部API（如计算器、网页搜索、代码执行环境）来弥补LLM在实时信息、精确计算和动作执行上的不足。工程权衡：这四个组件相辅相成。强大的规划需要消耗大量Token，复杂的记忆检索会增加延迟，而丰富的工具集则对LLM的指令遵循能力提出了极高要求。系统设计时需在功能丰富度与响应速度间取得平衡。

### 案例模拟

面试官追问：在实际开发中，这四个部分哪个最容易成为系统的瓶颈？回答：在我们的业务落地中，规划和工具调用的结合部最容易出问题。LLM经常能做出正确的规划，但在生成工具调用的JSON参数时出现格式错误或参数幻觉。为了解决这个问题，我们通常会引入结构化输出约束（如Function Calling微调）以及在代码层增加自动重试和错误反馈机制，让Agent能根据报错信息自我修正。

### 3. 2.1 介绍一下 规划（planning）？

- 主标签：Agent与工具调用
- 来源条数：1
- 答案生成方式：模型自动补全
- 来源：[LLMs_interview_notes / 大模型（LLMs）agent 面 / 未知](https://articles.zsxq.com/id_le02luntesap.html)

### 基础知识补充

- 任务分解将宏大目标拆解为可执行的原子级子任务。
- 逻辑推理通过思维链等方式推导任务执行的先后顺序。
- 动态调整根据环境反馈实时修改后续的执行计划。

### 详细解答

结论：规划是Agent将复杂目标转化为有序执行步骤的核心能力，包含任务分解和动态调整。原理：面对用户提出的复杂需求，Agent首先需要进行宏观规划。它利用LLM的推理能力，将大目标拆解为多个相互关联的子目标。在执行过程中，规划不仅是静态的步骤列表，还包含动态的自我反思。当某个子任务执行失败或获得新信息时，Agent能够评估当前状态，调整后续计划，甚至重新规划路径。工程权衡：强大的规划能力依赖于LLM的参数规模和推理深度。使用如Tree of Thoughts等复杂规划算法能显著提升任务成功率，但会成倍增加Token消耗和推理延迟。在工程实践中，通常对简单任务使用单步规划，对复杂任务才启用多步树状规划。

### 案例模拟

面试官追问：如果Agent在规划时陷入了死循环，一直重复相同的错误步骤，如何解决？回答：这种“规划死循环”在Agent开发中很常见。我们的解决方案是引入外部干预和状态监控机制。首先，在Prompt中强制要求Agent输出反思日志，对比当前状态与历史状态；其次，在代码层设置最大迭代次数和重复动作检测。一旦检测到连续执行相同动作且结果未变，系统会强制中断当前规划，注入错误提示，迫使Agent尝试备用路径或直接请求人类协助。

### 4. 2.1.1.1 如何进行 拆解子目标和任务分解？

- 主标签：Agent与工具调用
- 来源条数：1
- 答案生成方式：模型自动补全
- 来源：[LLMs_interview_notes / 大模型（LLMs）agent 面 / 未知](https://articles.zsxq.com/id_le02luntesap.html)

### 基础知识补充

- 提示词工程通过特定指令引导模型输出分步计划。
- 领域知识注入利用专家经验指导特定场景的任务拆解。
- 依赖关系图构建明确各个子任务之间的先后执行顺序。

### 详细解答

结论：任务分解主要通过精心设计的Prompt引导LLM，结合领域知识将复杂任务转化为有向无环图或线性步骤。原理：进行任务分解时，通常向LLM提供包含“思考过程”的系统提示词。例如，要求模型以“第一步、第二步”的格式输出，或者生成结构化的JSON格式计划。高级的分解方法会要求模型不仅列出任务，还要明确任务间的输入输出依赖关系，形成执行图。此外，对于特定垂直领域，可以向模型注入标准工作流（SOP）作为上下文，指导其进行更专业的拆解。工程权衡：细粒度的拆解有助于提高单步执行的成功率，但会导致整体流程过长，增加累积误差和延迟。粗粒度的拆解虽然高效，但对单步执行工具的能力要求极高。因此，拆解粒度需要根据底层LLM的能力和可用工具的复杂度进行动态调整。

### 案例模拟

面试官追问：如何评估Agent生成的任务分解计划是否合理？回答：在业务中，我们通常采用“双模型校验”机制。主Agent生成任务分解计划后，我们会调用另一个轻量级评估模型（或基于规则的校验器）来检查该计划。评估标准包括：步骤是否完整、依赖关系是否冲突、是否调用了不存在的工具等。如果校验不通过，会将错误信息反馈给主Agent要求重写。这种机制虽然增加了少量延迟，但大幅提升了复杂任务的执行成功率。

### 5. 2.1.1.2 拆解子目标和任务分解 有哪些方法？

- 主标签：Agent与工具调用
- 来源条数：1
- 答案生成方式：模型自动补全
- 来源：[LLMs_interview_notes / 大模型（LLMs）agent 面 / 未知](https://articles.zsxq.com/id_le02luntesap.html)

### 基础知识补充

- 思维链引导大语言模型逐步写出中间的逻辑推理过程。
- 思维树在每一步探索多个分支并进行启发式路径评估。
- 任务特定提示基于标准操作流程进行结构化任务拆解。

### 详细解答

结论：常见的任务分解方法包括思维链（CoT）、思维树（ToT）、Plan-and-Solve以及基于SOP的结构化拆解。原理：1. CoT：最基础的方法，通过“Let's think step by step”促使模型线性拆解任务。2. ToT：将拆解过程视为树状搜索，在每一步生成多个可能的子任务分支，并评估其可行性，选择最优路径。3. Plan-and-Solve：先让模型生成完整的计划列表，然后严格按照列表逐一执行。4. 基于SOP的拆解：在Prompt中提供标准操作流程，模型只需将当前任务映射到SOP的各个环节中。工程权衡：CoT实现简单、延迟低，适合常规任务；ToT逻辑严密、成功率高，但需要多次模型调用，成本和延迟极高，仅适用于高价值的复杂推理任务。实际工程中常采用Plan-and-Solve结合动态修正的折中方案。

### 案例模拟

面试官追问：在实际项目中，你会优先选择哪种任务分解方法？回答：我会根据任务的确定性来选择。对于确定性高、流程固定的业务（如数据报表生成），我会优先使用基于SOP的拆解，通过Few-shot Prompt让模型直接输出结构化步骤，稳定且高效。对于开放性强、探索性高的任务（如复杂Bug排查），我会采用Plan-and-Solve框架，先生成初步计划，但在执行每一步后加入状态评估，允许模型动态修改后续计划，以兼顾效率和灵活性。

### 6. 2.1.2.1 如何进行 模型自我反省？

- 主标签：Agent与工具调用
- 来源条数：1
- 答案生成方式：模型自动补全
- 来源：[LLMs_interview_notes / 大模型（LLMs）agent 面 / 未知](https://articles.zsxq.com/id_le02luntesap.html)

### 基础知识补充

- 状态对比分析当前执行结果与预期目标之间的差异。
- 错误归因通过分析报错信息找出执行失败的根本原因。
- 策略修正基于反思结果生成新的执行路径或调用参数。

### 详细解答

结论：模型自我反省通过将执行结果作为反馈输入给LLM，促使其评估偏差并调整后续策略。原理：自我反省是Agent实现闭环控制的关键。当Agent执行一个动作（如调用API）后，系统会捕获该动作的输出（如成功数据、报错信息或环境状态变化）。将这些反馈信息与原始目标一起重新构建为Prompt，要求LLM回答三个问题：1. 当前结果是否满足预期？2. 如果失败，原因是什么？3. 下一步应该如何调整？模型基于这些反思生成新的动作指令。工程权衡：自我反省显著提升了Agent的鲁棒性和容错率，使其能从错误中恢复。但频繁的反思会消耗大量Token，并可能导致上下文窗口溢出。因此，工程上通常只在关键节点或动作执行失败时才触发深度反省机制。

### 案例模拟

面试官追问：如果API返回的错误信息非常长且包含大量无用堆栈，模型反省时抓不住重点怎么办？回答：这是一个典型的工程痛点。直接将冗长的报错喂给LLM不仅浪费Token，还容易引发注意力分散。我们的做法是在反馈给LLM之前，先经过一层轻量级的“信息清洗”。使用正则表达式提取核心错误码和关键异常信息，或者使用一个较小的模型专门做错误摘要。只有经过提炼的、高信噪比的错误信息才会进入主Agent的反思Prompt中，从而提高反省的准确率。

### 7. 2.1.2.2 模型自我反省 有哪些方法？

- 主标签：Agent与工具调用
- 来源条数：1
- 答案生成方式：模型自动补全
- 来源：[LLMs_interview_notes / 大模型（LLMs）agent 面 / 未知](https://articles.zsxq.com/id_le02luntesap.html)

### 基础知识补充

- ReAct框架交替进行推理思考与动作执行以实现反思。
- Reflexion通过维护反思日志来指导未来的决策过程。
- 批评者模型引入独立大模型专门负责评估和指出错误。

### 详细解答

结论：主流的自我反省方法包括ReAct、Reflexion以及引入外部Critic模型的评估机制。原理：1. ReAct：将推理和执行交织，模型在每次行动前先输出一段思考，评估上一步结果并决定下一步，是最基础的反思形式。2. Reflexion：在ReAct基础上引入了长期记忆，模型在任务失败后会生成一段文字反思，并将其存储起来，在下次尝试时作为上下文提示，避免重复犯错。3. Critic机制：使用一个独立的、专门微调过的LLM作为“批评者”，专门审查主Agent的输出和计划，指出逻辑漏洞或工具使用错误，主Agent根据批评意见进行修改。工程权衡：ReAct轻量且易于实现，是目前工业界的标配。Reflexion适合需要反复试错的复杂环境，但记忆管理复杂。Critic机制效果最好，能有效缓解主模型的“盲目自信”，但会增加一倍的推理成本。

### 案例模拟

面试官追问：在多Agent系统中，如何利用反省机制提升整体表现？回答：在多Agent协作场景中，我们会将反省机制转化为“辩论与同行评审”。例如在代码生成任务中，Coder Agent负责写代码，Tester Agent负责运行测试并生成报错，Reviewer Agent负责分析报错并给出修改建议（即反省过程）。这种职责分离不仅降低了单个模型的认知负担，还能通过多视角的交叉验证，显著提升最终代码的质量和系统的稳定性。

### 8. 2.2 介绍一下 记忆（Memory）？

- 主标签：Agent与工具调用
- 来源条数：1
- 答案生成方式：模型自动补全
- 来源：[LLMs_interview_notes / 大模型（LLMs）agent 面 / 未知](https://articles.zsxq.com/id_le02luntesap.html)

### 基础知识补充

- 短期记忆通常指当前对话轮次中的上下文历史信息。
- 长期记忆利用向量数据库存储海量历史交互与知识。
- 记忆检索通过相似度计算提取与当前任务相关的经验。

### 详细解答

结论：Agent的记忆模块分为短期记忆和长期记忆，用于维持上下文连贯性和积累历史经验。原理：短期记忆受限于LLM的上下文窗口大小，通常通过滑动窗口或摘要机制保存当前任务的近期交互历史，确保Agent理解当前语境。长期记忆则突破了窗口限制，将历史对话、成功经验或外部知识库转化为向量表示，存储在向量数据库中。当Agent面临新任务时，通过语义检索召回相关的长期记忆，作为增强上下文注入到Prompt中。工程权衡：短期记忆响应快但容量有限；长期记忆容量无限但存在检索延迟和召回准确率问题。优秀的Agent系统需要设计高效的记忆读写策略，如定期将短期记忆总结归档为长期记忆，并结合关键词和向量混合检索提升召回质量。

### 案例模拟

面试官追问：随着交互增加，短期记忆越来越长，快要超出模型的Context Window时怎么处理？回答：在工程实践中，我们通常采用“分层压缩”策略。首先，保留最近N轮的完整对话以维持即时语境；对于更早的对话，调用LLM生成精简的摘要替换原文；如果摘要依然过长，则将最旧的摘要转化为向量存入长期记忆库，并从上下文中移除。这样既保证了Token消耗的可控，又不会丢失关键的历史信息。

### 9. 2.3 介绍一下 工具使用（tool use）？

- 主标签：Agent与工具调用
- 来源条数：1
- 答案生成方式：模型自动补全
- 来源：[LLMs_interview_notes / 大模型（LLMs）agent 面 / 未知](https://articles.zsxq.com/id_le02luntesap.html)

### 基础知识补充

- 工具描述通过清晰的文档告知模型工具的功能与参数。
- 格式化输出要求模型严格按照JSON等格式生成指令。
- 执行与反馈将外部API的真实返回结果重新输入给模型。

### 详细解答

结论：工具使用赋予了Agent与外部世界交互的能力，弥补了LLM在实时信息和精确计算上的短板。原理：工具使用的核心是让LLM理解何时以及如何调用外部API。首先，开发者需要在Prompt中提供工具的详细描述（包括功能、输入参数Schema和输出格式）。当LLM判断需要外部信息时，它会暂停文本生成，输出一个结构化的工具调用请求。系统拦截该请求，执行对应的本地代码或网络API，然后将执行结果作为新的观察附加到上下文中，交还给LLM继续推理。工程权衡：工具极大地扩展了Agent的能力边界，但也引入了稳定性风险。LLM可能会捏造不存在的工具，或者在API超时时不知所措。因此，必须在代码层实现严格的参数校验、超时控制和错误重试机制。

### 案例模拟

面试官追问：如果Agent需要调用的工具非常多（比如上百个API），全部放在Prompt里会导致Token超限，怎么解决？回答：面对海量工具，我们采用“工具检索”方案。我们将所有工具的描述文档向量化存储。当用户输入任务时，系统首先通过语义相似度检索出最相关的Top-K个工具，然后只将这K个工具的描述注入到Agent的Prompt中。这样不仅大幅节省了Token，还降低了LLM在众多工具中选择错误的概率，提升了指令遵循的准确性。

### 10. 三、大模型（LLMs）agent 主要 利用了 大模型 哪些能力？

- 主标签：Agent与工具调用
- 来源条数：1
- 答案生成方式：模型自动补全
- 来源：[LLMs_interview_notes / 大模型（LLMs）agent 面 / 未知](https://articles.zsxq.com/id_le02luntesap.html)

### 基础知识补充

- 自然语言理解能力用于精准解析用户意图和环境反馈。
- 逻辑推理与规划能力支撑复杂任务的拆解与步骤推导。
- 指令遵循与格式化输出确保工具调用的参数准确无误。

### 详细解答

结论：Agent主要利用了LLM的语义理解、逻辑推理、上下文学习和指令遵循四大核心能力。原理：1. 语义理解：LLM能够准确解析模糊的用户指令，并从冗杂的环境反馈中提取关键信息。2. 逻辑推理：这是Agent的“大脑”，通过思维链等方式，LLM能够进行因果推断、任务拆解和自我反思。3. 上下文学习：Agent无需重新训练，只需在Prompt中提供Few-shot示例或记忆片段，LLM就能快速适应新任务。4. 指令遵循：LLM能够严格按照预设的系统指令行事，特别是输出符合Schema规范的JSON格式，这是实现可靠工具调用的基石。工程权衡：不同能力对模型的要求不同。语义理解小模型即可胜任，但复杂的逻辑推理和稳定的格式化输出高度依赖千亿参数级的大模型。在实际业务中，常采用大小模型协同以平衡成本与效果。

### 案例模拟

面试官追问：在评估一个开源大模型是否适合做Agent时，你最看重它的哪个指标？回答：我最看重它的“指令遵循”和“格式化输出”能力。即使一个模型的知识渊博，如果它不能稳定地输出符合JSON Schema的工具调用参数，或者总是忽略Prompt中的约束条件，它在Agent系统中就会频繁引发解析错误，导致流程中断。因此，在选型时，我会优先测试模型在复杂Function Calling场景下的成功率和鲁棒性。

## 函数调用 Function Call 篇

### 11. 一、为什么需要 函数调用(function call)？

- 主标签：Agent与工具调用
- 来源条数：1
- 答案生成方式：模型自动补全
- 来源：LLMs_interview_notes / 函数调用 Function Call 篇 / 未知

### 基础知识补充

- 弥补大模型无法获取实时信息和私有数据的缺陷
- 解决大模型在复杂数学计算和逻辑推理上的幻觉
- 赋予大模型与外部环境交互并执行实际动作的能力

### 详细解答

结论：函数调用使大模型从单纯的文本生成器进化为能够执行实际任务的智能代理（Agent）。 原理与权衡：大语言模型的知识受限于训练数据的时间截断，且缺乏与外部世界交互的手段。通过引入函数调用，模型可以在需要时暂停生成，输出结构化的参数请求外部API执行特定任务（如查询天气、执行代码、操作数据库），然后将API返回的结果整合到最终回答中。这种机制极大地扩展了模型的能力边界，降低了幻觉，但也带来了额外的延迟和API调用成本，工程上需要权衡调用的频次和准确性。

### 案例模拟

面试官追问：如果模型频繁调用错误的函数怎么办？ 回答：首先，我会优化系统提示词，明确每个函数的用途和参数格式。其次，在工程侧增加参数校验逻辑，如果模型生成的参数不合法，直接返回错误信息让模型重试。最后，可以通过微调专门提升模型的函数调用能力，构建包含正确调用示例的高质量数据集进行训练。

### 12. 二、什么是 函数调用(function call)？

- 主标签：Agent与工具调用
- 来源条数：1
- 答案生成方式：模型自动补全
- 来源：LLMs_interview_notes / 函数调用 Function Call 篇 / 未知

### 基础知识补充

- 允许大模型输出符合特定JSON格式的结构化数据
- 包含函数声明、参数生成、外部执行和结果整合
- 是实现ReAct框架和复杂Agent工作流的核心机制

### 详细解答

结论：函数调用是大语言模型理解用户意图并生成结构化指令以调用外部工具的机制。 原理与权衡：在对话过程中，开发者将可用函数的描述（名称、功能、参数Schema）作为上下文传递给模型。当模型判断需要外部信息时，它会停止生成普通文本，转而输出一个包含函数名和参数的JSON对象。应用程序解析该JSON，在本地或服务器执行对应函数，并将执行结果作为新的消息返回给模型，模型据此生成最终回复。这种机制要求模型具备强大的指令遵循和格式化输出能力，工程实现上需要严格的Schema校验以防止执行异常。

### 案例模拟

业务案例模拟：在智能客服系统中，用户询问“帮我查一下订单号123的物流状态”。系统将get_logistics(order_id)函数描述传给模型。模型识别意图，输出{"name": "get_logistics", "arguments": {"order_id": "123"}}。后端执行该函数获取物流信息“已发货”，将其返回给模型，模型最终回复“您的订单123已发货，请耐心等待”。

### 13. 开源模型 Function Call 方案有哪些？

- 主标签：Agent与工具调用
- 来源条数：1
- 答案生成方式：模型自动补全
- 来源：LLMs_interview_notes / 函数调用 Function Call 篇 / 未知

### 基础知识补充

- 基于提示词工程的零样本或少样本工具调用方案
- 通过特定格式微调提升指令遵循能力的开源模型
- 采用外挂解析器或语法约束的结构化输出生成框架

### 详细解答

结论：开源模型的Function Call方案主要分为提示词工程、模型微调和推理时约束三种路径。 原理与对比：1. 提示词工程（如ReAct）：通过System Prompt详细描述工具格式，依赖模型自身的涌现能力，实现简单但稳定性较差。2. 模型微调（如ToolLLaMA、Qwen-Agent）：使用大量工具调用轨迹数据对开源模型进行SFT，使其原生支持特定格式的函数调用，准确率高但训练成本大。3. 推理时约束（如Guidance、Outlines）：在解码阶段通过正则表达式或有限状态机强制模型输出合法的JSON参数，能彻底解决格式错误问题，但可能影响推理速度。工程上常结合微调与推理约束。

### 案例模拟

面试官追问：你们项目中选择了哪种方案？为什么？ 回答：我们采用了模型微调结合推理时约束的混合方案。业务场景对工具调用的准确率要求极高，单纯的提示词工程经常出现JSON格式错误或参数遗漏。我们先用开源的Qwen模型，因为它本身经过了工具调用微调，然后在推理端接入了Outlines库，强制约束输出符合JSON Schema，彻底杜绝了解析失败的问题。
