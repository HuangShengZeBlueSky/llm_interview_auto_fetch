# LLMs_interview_notes 提取：十六、LLMs Tokenizer 篇

> 来源仓库：https://github.com/km1994/LLMs_interview_notes
> 说明：该仓库大量内容以“题目目录 + 外链答案”形式存在，本页保留全量题目清单，并优先复用本仓库现成答案，再补充专题级总结。

## 专题总结

## 本专题考什么
- **底层算法理解**：考察对主流 Subword 分词算法（特别是 BPE）运作机制的深刻理解，而非仅仅停留在调用 API 的层面。
- **词表工程能力**：考察在实际业务中（如开源模型本土化）进行词表扩充（如中文 Tokenizer 构建）的动机与工程实现细节。
- **全链路数据处理经验**：考察对 LLM 训练生命周期（预训练 -> 继续预训练 -> 指令微调）中，数据拼接、特殊 Token 插入、Loss 掩码（Mask）等预处理细节的掌握程度。

## 答题主线
- **算法层**：从“字符/字节”到“子词”的演进逻辑，强调 BPE 基于**高频相邻对合并**的核心思想。
- **工程层（中文扩词表）**：遵循“痛点（编码效率低/语义割裂） -> 方案（训练专属词表并合并） -> 善后（Embedding 层扩充与初始化）”的逻辑。
- **链路层（不同阶段的数据构造）**：
  - **预训练/继续预训练**：强调**吞吐量与上下文利用率**（Document Packing，用 `&lt;eos&gt;` 拼接截断）。
  - **指令微调（SFT）**：强调**对话格式对齐与 Loss 计算范围**（Prompt 拼接特殊 Token，且 Prompt 部分不计算 Loss）。

## 代表题答法

### 问题1：介绍一下 Byte-Pair Encoding (BPE) 及其词典构建过程？
**答题要点**：
BPE 是一种介于字符级和词级之间的 Subword（子词）分词算法，旨在解决 OOV（未登录词）问题并平衡序列长度。
**构建过程（训练阶段）**：
1. **初始化**：将语料库中所有单词拆分为基础字符（或字节，即 BBPE），并在词尾添加特殊停止符（如 `&lt;/w&gt;`），统计每个单词的词频。
2. **统计频次**：统计当前词表中所有相邻 Token 对的出现频次。
3. **合并高频对**：找到频次最高的相邻 Token 对（如 `e` 和 `s`），将其合并为新的 Token（`es`），并加入词表。
4. **迭代**：重复步骤 2 和 3，直到词表大小达到预设的超参数（Vocab Size）或最高频次为 1 为止。

### 问题2：为什么需要构建中文 Tokenization？如何进行原始数据预处理？
**答题要点**：
**为什么需要**：
1. **编码效率低**：原版开源模型（如 LLaMA）中文词表极小，中文常被回退为 Byte 级别编码，导致一个汉字被切分为 2-3 个 Token，序列长度暴增，降低训练和推理效率。
2. **语义割裂**：中文字词被强行打碎，模型难以学习到完整的中文词汇语义。

**如何预处理（扩充词表）**：
1. **语料准备**：收集高质量、多样化的中文语料（百科、新闻、书籍等）。
2. **训练中文词表**：使用 SentencePiece 等工具在中文语料上单独训练一个中文 BPE 词表（通常大小在 2w-5w 之间）。
3. **词表合并**：将新训练的中文词表与原模型词表合并，去重。
4. **模型调整**：调整模型的 Embedding 层和 LM Head 层大小，新增 Token 的权重通常用原模型已有 Token 的均值或正态分布进行初始化。

### 问题3：为什么需要继续预训练（CPT）？数据如何预处理？
**答题要点**：
**为什么需要**：
当基础模型缺乏特定领域知识（如医疗、法律、代码）或特定语言能力（如中文）时，需要通过继续预训练注入领域知识，这比从头预训练成本更低。

**数据预处理方式**：
1. **清洗与分词**：对领域文档进行去重、清洗，并使用 Tokenizer 转化为 Token ID 序列。
2. **文档拼接（Document Packing）**：为了最大化 GPU 算力利用率，通常不会对单篇短文档进行 Padding，而是将多篇文档首尾相连，中间用 `&lt;eos&gt;`（End of Sequence）分隔。
3. **截断**：将拼接后的长序列按照模型的 Max Context Length（如 4096）进行等长截断，生成训练样本。

### 问题4：为什么需要指令微调（SFT）？数据如何处理？
**答题要点**：
**为什么需要**：
预训练模型只是“文字接龙”机器，SFT 的目的是让模型**对齐人类意图**，学会以问答、指令遵循的格式与人类交互。

**数据预处理方式**：
1. **格式化拼接**：将用户的 Instruction（指令）和模型的 Response（回复）按照特定的模板拼接，并插入角色特殊 Token（如 `<|im_start|>user\n...<|im_end|>`）。
2. **Tokenize 与 Padding**：将拼接好的对话转化为 Token ID，对齐到固定长度（Padding 通常补在左侧或右侧，用 `&lt;pad&gt;` 填充）。
3. **Loss 掩码（Masking）**：**这是 SFT 最核心的处理**。在构建 Label 时，将 Instruction 部分和 Padding 部分的 Label 设为 `-100`（PyTorch 中 CrossEntropyLoss 的默认 ignore_index），**只对 Response 部分的 Token 计算交叉熵损失**，强制模型学习“如何回答”而不是“复述问题”。

## LLMs Tokenizer 篇

- 来源链接：https://articles.zsxq.com/id_6z8ptdwqeid7.html
- 题目数：9

### 原始题目

1. 1 介绍一下 Byte-Pair Encoding(BPE) ？
2. 2 Byte-Pair Encoding(BPE) 如何构建词典？
3. 3 Byte-Pair Encoding(BPE) 具有什么优点？
4. 4 Byte-Pair Encoding(BPE) 具有什么缺点？
5. 5 手撕 Byte-Pair Encoding(BPE) ？
6. 1 介绍一下 Byte-level BPE ？
7. 2 Byte-level BPE 如何构建词典？
8. 3 Byte-level BPE 具有什么优点？
9. 4 Byte-level BPE 具有什么缺点？

## 怎么让英文大语言模型支持中文？（一） —— 构建中文tokenization

- 来源链接：https://articles.zsxq.com/id_w0d2q29sueq7.html
- 题目数：3

### 原始题目

1. 一、为什么需要 构建中文tokenization？
2. 二、如何对 原始数据预处理？
3. 三、如何构建中文的词库？

## 怎么让英文大语言模型支持中文？（二） —— 继续预训练篇

- 来源链接：https://articles.zsxq.com/id_jprkwhrvf3tw.html
- 题目数：4

### 原始题目

1. 一、为什么需要进行继续预训练？
2. 二、如何对 继续预训练 数据预处理？
3. 三、如何 构建模型？
4. 四、如何 使用模型？

## 怎么让英文大语言模型支持中文？（三） —— 对预训练模型进行指令微调

- 来源链接：https://articles.zsxq.com/id_p2wj7zadwxwb.html
- 题目数：5

### 原始题目

1. 一、为什么需要对预训练模型进行指令微调？
2. 二、对预训练模型进行指令微调 数据 如何处理？
3. 三、对预训练模型进行指令微调 tokenization 如何构建？
4. 四、对预训练模型进行指令微调 模型 如何构建？
5. 五、是否可以结合 其他库 使用？
